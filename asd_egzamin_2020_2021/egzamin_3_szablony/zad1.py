from zad1testy import runtests


def mr( X ):
    """tu prosze wpisac wlasna implementacje"""
    n = len(X)
    max_len = 1
    max_start = 0
    malejace = [1 for _ in range(n)]
    rosnace = [1 for _ in range(n)]
    mieszane = [1 for _ in range(n)]
    i = 0
    
    while max_len + i < n:


        i+=1
    return []

    
runtests( mr )


